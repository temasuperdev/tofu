# Example Environment Configuration
# Copy to .env.local and customize for your setup

# === LOCAL DOCKER REGISTRY ===
# Settings for local Docker Registry (registry.sh)
REGISTRY_HOST=localhost:5000
REGISTRY_PORT=5000
REGISTRY_CONTAINER_NAME=local-docker-registry
REGISTRY_IMAGE=registry:2

# === APPLICATION IMAGE ===
# Docker image name and tag
IMAGE_NAME=demo-app
IMAGE_TAG=latest
REGISTRY_VOLUME_NAME=registry-data

# === KUBERNETES (K3s) ===
# K3s cluster settings
NAMESPACE=demo-app
DEPLOYMENT_NAME=demo-app
K3S_SERVER=https://localhost:6443

# === PORT FORWARDING ===
# Ports for local access
LOCAL_PORT=8080           # Your local port
SERVICE_PORT=80           # Service port inside K3s

# === FLASK APPLICATION ===
# Flask app settings
APP_VERSION=1.0.0
ENVIRONMENT=development
FLASK_PORT=5000
PYTHONUNBUFFERED=1

# === DATABASE CONFIGURATION ===
# PostgreSQL database settings
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=notes_db
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/notes_db

# === FEATURE FLAGS ===
# Enable/disable components
ENABLE_REGISTRY=true
ENABLE_K3S=true
ENABLE_DOCKER_COMPOSE=true
ENABLE_TESTS=true

# === GITHUB (if using GitHub Actions) ===
# Only needed if pushing to GitHub Actions
GITHUB_REGISTRY=ghcr.io
GITHUB_USERNAME=your-username

# === BUILD OPTIONS ===
# Docker build options
DOCKERFILE=./docker/Dockerfile
BUILD_CONTEXT=.
CACHE_FROM_REGISTRY=false
PUSH_TO_REGISTRY=true
