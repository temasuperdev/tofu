{{- if and .Values.frontend.ingress.enabled (eq .Values.frontend.ingress.className "traefik") .Values.traefik.middlewareEnabled }}
# HTTP to HTTPS redirect через отдельный ingress (опционально)
# Можно использовать вместо этого Traefik redirect middleware
{{- end }}